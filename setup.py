from setuptools import setup, find_packages

setup(
    name="ckbvm-stock-data-service",
    version="1.0.0",
    author="Girish B",
    description="CKBVM - platform for extract all the history of nse market",

    packages=find_packages(),
    classifiers=[
        "Operating System :: OS Independent",
    ],
    install_requires=[],
)